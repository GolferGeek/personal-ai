{
  "name": "@personal-ai/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared configuration for Personal AI monorepo",
  "main": "index.js",
  "workspaces": [
    "eslint",
    "jest",
    "typescript"
  ],
  "scripts": {
    "build": "echo \"No build step required for config package\" && exit 0",
    "clean": "rm -rf node_modules"
  }
} 